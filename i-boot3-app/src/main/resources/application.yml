spring:
  application:
    name: i-boot3

  profiles:
    include:
      - dubbo
      - nacos
      - redis
      - datasource
      - mybatis

# 解决 cloud 版本兼容问题
  cloud:
    compatibility-verifier:
      enabled: false